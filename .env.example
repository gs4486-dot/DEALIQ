# Copy this to .env and fill in your values
ANTHROPIC_API_KEY=sk-ant-...

# Local dev only — points frontend at the Express server
# In production (Render) this is not needed (same-origin)
VITE_API_URL=http://localhost:3001
